import 'package:flutter/material.dart';

class AppColors {
  //here the Colors.indigo is used
  // static const Color primary = Color.fromRGBO(13, 141, 144, 1.0);
  static Color primary = Colors.indigo;
  static const Color secondary = Colors.white;
  static const Color black = Color.fromARGB(255, 27, 26, 26);
  // Add more color constants if needed
}
